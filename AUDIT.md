# `docs-to-book` 实际使用审计

审计对象：`lili-luo/aicoding-cookbook` 中的
`skills/claude code/docs-to-book`。审计基于仓库当前 `main` 内容、脚本静态
阅读和一次初始化/构建/校验演练；结论关注“真实项目接入时会不会误导或失效”，
不是对作者的代码风格评价。

## 保留的设计

- 按读者认知顺序重排文档，而不是照搬 `docs/` 目录。
- 把文档完备、残缺、无文档分成三条梳理路径。
- 将事实核对、漂移清单、浏览器渲染和视觉终检纳入交付流程。
- 用 `book.config.js` 作为页面顺序与导航的单一来源。

## 风险与改进

| 优先级 | 实际问题 | 使用时的后果 | 本项目的处理 |
| --- | --- | --- | --- |
| P0 | 原仓库根目录没有通用许可证，`docs-to-book` 目录也没有独立版权声明 | 直接复制模板再公开发布，来源与再分发边界不清 | 独立重写脚本和模板，保留灵感链接，不复制实现 |
| P1 | 缺少内容片段时 `build.js` 写入占位页并以成功结束 | CI 或交付说明会把“不完整手册”当成完成品 | `build_handbook.py` 默认预检并失败；`--draft` 明确标记预览 |
| P1 | 标题、摘要、分组名、来源路径直接拼入 HTML | 引号、`</script>` 或恶意片段会破坏页面或造成注入 | 生成元数据统一 HTML 转义；内容片段拒绝脚本、内联事件和 `javascript:` |
| P1 | 依赖 CommonJS `require()`，遇到宿主项目 `type: module` 需要手工改扩展名 | 同一份模板在不同项目中构建结果不一致 | 使用 Python 标准库脚本，消除 Node 模块模式依赖 |
| P1 | Mermaid 库不随 skill 提供，真实渲染步骤依赖外部浏览器和未固定版本 | “零依赖”站点在离线环境可能只有空白图 | Mermaid 变为显式可选项；缺本地库直接失败，优先支持本地 SVG |
| P1 | `verify.js` 主要依赖正则，不能完整解析相对链接、重复 ID、旧页面和单引号属性 | 校验通过但导航仍可能死链，重建后旧页面残留 | verifier 解析生成页面路径、锚点、资产、旧页面和证据 manifest |
| P2 | 搜索索引只保留每页前 2400 个字符 | 长页面后半段搜不到 | 生成完整正文索引，并在浏览器端限制结果数量而非截断事实 |
| P2 | 配置没有 slug、home 页、必填字段和重复项校验 | 运行到 `target.url` 等位置才抛出难定位异常 | 构建前校验 JSON schema 的关键不变量并输出具体路径 |
| P2 | 客户端 Mermaid 异常被空 `catch` 吞掉，复制和搜索失败也缺少可见反馈 | 图坏了但交付者不知道为什么 | 浏览器脚本对不可用存储和搜索加载打印可见警告/错误 |
| P2 | 顶层 README 与实际树不同步，未介绍新加入的 skill | 用户无法从仓库入口发现主要能力 | 作品集 README 直接说明能力、取舍、边界和运行命令 |

## 尚未自动化的边界

事实 manifest 只能检查“声明的 token 是否出现在目标页面”，不能证明 token
与源码语义一致；这一步仍需在写作时对照可执行代码。静态 verifier 也不能
证明浏览器布局、字体、Mermaid 版本行为，必须保留一次真实浏览器烟测记录。
