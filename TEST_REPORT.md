# 实际素材演练记录

公开仓库只保留可复现的合成示例和通用脚本。真实项目目录、私有手册和启动端口
不进入本仓库。

## 2026-09-15 一问一图与竖向流程图（0.9.0）

默认交付改为：一个读者问题 + 可选的客户端 / 服务端 / 文档目录 → 一份可核验 HTML 图解。

- 首页是可切换竖向流程图：多入口切换专属段，再读共用段。
- 系统地图留在项目地图页；15/30/60 分钟路径留在阅读指南。
- 节点标题写顺序；证据缺口只留状态标记，不写进标题。
- 合成示例：`project-handbook/assets/knowledge.example.json`。
- 问答默认本地证据摘录（`--evidence-only`），不调用模型。
- 渲染器拒绝覆盖已有输出目录。

回归：`python -m unittest discover -s tests -v`。私有项目复测留在仓库外。

### 发布前复核

- Python 回归：29 项通过。
- 三组浏览器脚本测试通过：导航、问答连续性与等待反馈、地图与流程切换。
- 合成示例构建：8 页、28 个搜索区段；静态验证通过。
- 修复测试缩进，以及地址留空时无法使用本地 relay 获取模型和提问的问题。
- 三张 README 配图为已有生成示意图，不是真实项目截图。
- 发布范围扫描未发现已知私有项目标识、本机私有路径或常见密钥格式；本地设计笔记不发布。
- 本轮模型请求使用模拟接口，不代表真实供应商连通性或答案正确性验证。

## 2026-09-03 目录导入演练

本次使用用户提供的客户端代码目录和文档/配置目录做了本机临时演练。为了避免
真实资料进入公开作品集，导入结果写入系统临时目录，未加入 Git、未上传
GitHub，演练结束后可自行删除。

### 输入范围

- 客户端代码：用户本机提供的独立目录（服务器代码未提供，因此明确排除）
- 文档与配置：用户本机提供的独立目录，包含 Excel、CSV、脚本和策划资料

### 结果

- 扫描客户端文件：60,017 个
- 扫描文档/配置文件：4,135 个
- 每类抽样展示：30 个
- 生成页面：5 页
- 静态验证：通过（页面、链接、资产、锚点、事实清单）

### 交互回归

- 桌面端：左侧导航、中间正文、右侧问答面板同时可见
- 窄屏：导航折叠，问答面板移动到正文之后且仍可见
- 已有证据的问题：能够进入 relay 请求流程
- 中文无关问题：不会因单字误匹配而浪费模型请求，会提示更换关键词或重新导入
- 缺少模型：提示填写模型或设置 `OPENAI_MODEL`
- 缺少 API Key：提示在启动 relay 的同一个 PowerShell 窗口设置环境变量并重启
- 直接双击 `file://` 页面：提示改用 relay 输出的 localhost 地址
- 浏览器控制台：未发现未处理错误

### 尚未声称完成的部分

- 本次没有调用真实模型接口，因此未伪造“模型答案正确”的结论。
- 当前检索是关键词基线，不是向量数据库或生产级混合检索。
- 静态 HTML 不能自动继承 Codex 当前会话 token；需要宿主提供受控 relay 才能由
  agent 统一管理模型权限和费用。
